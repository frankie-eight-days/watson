# @watson/sandbox-runner

Cloudflare Sandbox SDK execution service — owned by Tab C.

Runs the Vending-Bench fork end-to-end in Sandbox SDK containers (clone → install →
run N steps → emit metric JSON) plus the GitHub PR machinery (branch/commit/PR helpers).
