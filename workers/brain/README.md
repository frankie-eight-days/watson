# @watson/brain

Hermes DO + workflow agents — owned by Tab B.

Hermes president agent in a Cloudflare Durable Object (Agents SDK), the Bench WebSocket
chat protocol, and the OpenAI Agents SDK workflow Workers (watercooler ingestion, library
pipeline, lab experiment loop). Every agent step goes through emitEvent.
